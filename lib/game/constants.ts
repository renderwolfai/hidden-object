export const ANIMATION_DURATION = 600; // Slightly faster animation
export const DEBUG_MODE = false;

export const GAME_CONSTANTS = {
  MIN_SCALE: 0.3, // Smaller end scale
  ARC_HEIGHT: 50, // Reduced arc height
  ANIMATION_FADE_START: 0.5, // Start fading earlier
} as const;